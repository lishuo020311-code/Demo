// vite.config.js

import { defineConfig } from 'vite';

export default defineConfig({
    // 保持配置对象为空即可，因为您的项目目前不需要特殊配置
    // Vite 默认的配置（如处理 .js, .css 文件）已经足够了。

    // 您也可以添加一个基础路径配置，以防您在其他地方部署：
    // base: './', 
    base: '/demo/',
    plugins: []
});